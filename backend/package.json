{
  "name": "illusion-vpn-backend",
  "version": "0.1.0",
  "description": "Mock backend for IllUsion VPN: server list, auth, session provisioning.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2"
  }
}
